name: Stale (Disabled)

on:
  workflow_dispatch:

permissions: {}

jobs:
  disabled:
    name: Stale automation disabled
    runs-on: ubuntu-latest
    steps:
      - name: Explain status
        run: |
          echo "The upstream OpenClaw stale automation is intentionally disabled in this fork."
          echo "It depended on upstream GitHub App secrets, labels, runners, and closure policies."
